/// Default page margin in mm
pub const DEFAULT_PAGE_MARGIN: f32 = 15.0;

/// Default hand block width in mm
pub const DEFAULT_HAND_WIDTH: f32 = 45.0;

/// Default hand block height in mm (4 lines * 5mm line height = 20mm)
pub const DEFAULT_HAND_HEIGHT: f32 = 20.0;

/// Default gap around compass rose in mm
pub const DEFAULT_COMPASS_GAP: f32 = 12.0;

/// Default line height in mm
pub const DEFAULT_LINE_HEIGHT: f32 = 5.0;

/// Default title font size in points
pub const DEFAULT_TITLE_FONT_SIZE: f32 = 14.0;

/// Default header font size in points
pub const DEFAULT_HEADER_FONT_SIZE: f32 = 11.0;

/// Default body font size in points
pub const DEFAULT_BODY_FONT_SIZE: f32 = 10.0;

/// Default card/diagram font size in points
pub const DEFAULT_CARD_FONT_SIZE: f32 = 11.0;

/// Default bidding column width in mm
pub const DEFAULT_BID_COLUMN_WIDTH: f32 = 24.0;

/// Default bidding row height in mm
pub const DEFAULT_BID_ROW_HEIGHT: f32 = 5.0;

/// RGB color for black suits (spades, clubs)
pub const BLACK_SUIT_COLOR: (f32, f32, f32) = (0.0, 0.0, 0.0);

/// RGB color for red suits (hearts, diamonds)
pub const RED_SUIT_COLOR: (f32, f32, f32) = (0.8, 0.0, 0.0);
